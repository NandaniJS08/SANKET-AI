/**
 * Application-wide constants
 * Single source of truth for magic strings and configuration values.
 */

/** localStorage key used to persist the authenticated user session. */
export const STORAGE_KEY_USER  = "infrawatch_user";

/** localStorage key used to persist the user's theme preference. */
export const STORAGE_KEY_THEME = "sanket_theme";

/** Custom DOM event dispatched when the user logs out. */
export const LOGOUT_EVENT = "infrawatch_logout";

/** Application display name. */
export const APP_NAME = "SANKET-AI";

/** Application subtitle shown in headers. */
export const APP_SUBTITLE = "MoSPI · IPMD";
